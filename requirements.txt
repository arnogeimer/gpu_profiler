# Pinned to the exact CUDA build so cuDNN/cuBLAS/cutlass (bundled in the wheel) — and thus the
# kernel vocabulary — are identical on every GPU. The +cu130 wheels live on PyTorch's index.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.11.0+cu130
timm>=1.0.27
torchvision==0.26.0+cu130
huggingface-hub>=1.0
nvidia-ml-py>=12.0
transformers>=4.46
pandas>=2.0
diffusers>=0.30
accelerate>=0.30
pillow>=10.0
numpy>=1.24
peft>=0.10
